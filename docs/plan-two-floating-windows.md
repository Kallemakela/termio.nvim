# Plan: two floating windows

- First row starts at prompt, this shows buffer line 1
- Second-n rows starts at start, this shows buffer lines 2-n
- Start with always having two windows, later auto create another

### Unknowns
- How does wrap work if one terminal buffer is open in two differently sized windows 
